{
  "name": "burner",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "preinstall": "npx only-allow yarn",
    "dev": "yarn --cwd client dev",
    "build": "yarn --cwd client build",
    "preview": "yarn --cwd client preview"
  }
}
